export default function RegisterPage() {
  return (
    <section className="page-card">
      <p className="eyebrow">Authentication</p>
      <h1>Register</h1>
      <p>
        Registration form will create STUDENT accounts and store the JWT token
        in the browser in the next frontend auth stage.
      </p>
    </section>
  );
}